<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8" />
    <title>Detalhes do Aluno</title>
    <link rel="stylesheet" href="/css/estilo.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <%- include("partials/cabecalho") %>

    <div class="container mt-4">
        <h1>Detalhes do Aluno</h1>
        <dl class="row">
            <dt class="col-sm-3">Matrícula:</dt>
            <dd class="col-sm-9"><%= aluno.matricula %></dd>

            <dt class="col-sm-3">Nome:</dt>
            <dd class="col-sm-9"><%= aluno.nome %></dd>

            <dt class="col-sm-3">Disciplina:</dt>
            <dd class="col-sm-9"><%= aluno.disciplina ? aluno.disciplina.nome : '---' %></dd>

            <% // Assuming 'status' would come from a Matricula, not directly on Disciplina as per your schemas.
               // If you want to show Matricula status, you'd need to populate Matricula in the route too.
               // For now, removing the conditional check for aluno.disciplina.status as it's not defined there.
            %>
        </dl>

        <a href="/tabela" class="btn btn-secondary">Voltar</a>
        <a href="/alunos/editar/<%= aluno._id %>" class="btn btn-primary">Editar</a>
    </div>

</body>
</html>